# Technical Debt

| ID | Description | Reason | Impact | Risk | Priority | Proposed Resolution | Status |
|---|---|---|---|---|---|---|---|
| DEBT-001 | SQL analysis + reform use regex/token heuristics, not a real BigQuery parser (`analyzer.ts`, `reformer.ts`). | Fast to build; sufficient for advisory. | May miss exotic constructs (alias shadowing, dynamic SQL, CTE renames); reformed SQL (sde_decrypt wrapping) is best-effort and must be reviewed before running. | Medium | Medium | Integrate a real BigQuery SQL AST parser for filter detection + safe rewriting. | Open |
| DEBT-002 | ID-token verification uses `jose` + Google JWKS rather than the Firebase Admin SDK. | Admin SDK is heavy / needs a service account inside the Worker. | Must keep issuer/audience checks correct; no revocation check. | Low | Low | Revisit if session revocation becomes a requirement. | Accepted |
| DEBT-003 | Frontend JS bundle > 500 kB (Firebase SDK). | Hackathon scope. | Slightly slower first load. | Low | Low | Code-split Firestore/Auth, or use modular dynamic imports. | Open |
| DEBT-004 | Metadata currently covers a single built-in table (`triumph_transactions`). | Only sample provided. | Unknown tables reported as "unknown" (no findings). | Low | Low | Add more via the "Add metadata" feature (shared `sharedMetadata`) or bundled files. | Mitigated |
| DEBT-005 | The client sends ALL shared metadata (and reformed examples) in each `/api/analyze` request. | Keeps the Pages Function stateless (no Firestore credentials in the Worker). | Request grows with the library; fine for hundreds, not thousands. | Low | Low | Move reads server-side or scope to tables referenced by the SQL. | Open |
| DEBT-006 | Admin allowlist exists in two files (`functions/_lib/admins.ts` + `firestore.rules`). | Rules can't import TS; need DB-level enforcement too. | Must edit both to add/remove an admin. | Low | Low | Generate rules from the list at build, or move admins to a Firestore doc referenced by rules. | Open |
