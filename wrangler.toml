# Cloudflare Pages configuration.
# The frontend builds to ./dist; Functions are auto-detected from ./functions.
# Secrets (GEMINI_API_KEY, FIREBASE_PROJECT_ID) and the public VITE_FIREBASE_*
# build vars are set in the Cloudflare Pages dashboard, NOT here.
name = "ascend-ai"
compatibility_date = "2025-07-18"
pages_build_output_dir = "dist"
