# BUGS AND BLOCKERS

## Blockers
- **B-001 — End-to-end run needs credentials.** Verifying live sign-in,
  `/api/analyze`, and Firestore history requires a Firebase project +
  `GEMINI_API_KEY`. Severity: low (build/tests pass; logic verified by unit
  tests). Workaround: run locally once credentials are provided (see HANDOFF).

## Active bugs
- None known.
