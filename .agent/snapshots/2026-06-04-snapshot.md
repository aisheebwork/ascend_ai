# Project Snapshot — 2026-06-04

Milestone: initial build + "Add metadata" feature complete.

## Feature snapshot
| ID | Feature | Status |
|---|---|---|
| FEATURE-001 | Google Sign-In (Firebase Auth) | Implemented |
| FEATURE-002 | SQL/HQL upload & paste | Implemented |
| FEATURE-003 | PII / sensitive column detection (Flow 1) | Implemented |
| FEATURE-004 | Partition-filter detection (Flow 2) | Implemented |
| FEATURE-005 | Suggestion generation (Gemini + rule-based fallback) | Implemented |
| FEATURE-006 | Per-user analysis history | Implemented |
| FEATURE-007 | Shared metadata library ("Add metadata") | Implemented |

## Architecture snapshot
React+Vite+Tailwind SPA (Cloudflare Pages) → `/api/analyze` Pages Function
(verify Firebase ID token via jose/JWKS → deterministic analyzer over built-in +
shared metadata → Gemini suggestion text with fallback) → Firestore
(`users/{uid}/analyses`, `sharedMetadata`) written client-side.

## Repository structure snapshot
```
src/ (SPA), functions/_lib + functions/api (Pages Function), metadata/ (raw),
scripts/build-metadata.mjs, test/, docs/ (+adr/), .agent/ (+snapshots/),
firestore.rules, wrangler.toml, .env.production
```

## Dependency snapshot
react, react-dom, firebase, jose | dev: vite, @vitejs/plugin-react, tailwindcss,
postcss, autoprefixer, wrangler, tsx, typescript.

## Data snapshot
Built-in metadata: `triumph_transactions` — 45 cols, `require_partition_filter=true`,
partition `date_stmt_yr`, PII `cm13` (NGBD-SDE-CM13), `cm15` (NGBD-SDE-CM15).

## Verification at snapshot
`tsc --noEmit` clean; `vite build` succeeds; 10/10 unit tests pass; wrangler
Pages Function smoke test returns 401/405 on the auth gate as designed.
